const inputData = "2 4 4"; 

const [t1, t2, t3] = inputData.trim().split(/\s+/).map(Number);

const totalSpeed = (1 / t1) + (1 / t2) + (1 / t3);
const totalTime = 1 / totalSpeed;

console.log(totalTime.toFixed(2));
